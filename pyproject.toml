[project]
name = "primo-agente"
version = "0.1.0"
description = "Agentic SOC triage — 10 scenari dataset_test, ReAct, RAG, SQLite LTM, report HTML"
dependencies = [
    "chromadb>=0.5",
    "openai",
    "python-dotenv",
    "pydantic",
]

[project.optional-dependencies]
test = [
    "pytest",
    "pytest-cov"
]
dev = [
    "ruff"
]

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]